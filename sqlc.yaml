version: "2"
sql:
  - schema: "database/migrations"
    queries: "database/query"
    engine: "postgresql"
    gen:
      go:
          package: "database"
          out: "database/sqlc"
          sql_package: "pgx/v5"
          emit_json_tags: true
          emit_prepared_queries: false
          emit_interface: true
