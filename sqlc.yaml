version: "2"
sql:
  - schema: "database/migration"
    queries: "database/query"
    engine: "postgresql"
    gen:
      go:
          package: "database"
          out: "database/sqlc"
          emit_json_tags: true
          emit_prepared_queries: false
          emit_interface: true
